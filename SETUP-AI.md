# คู่มือผู้ช่วย AI ของ FlowDesk

ระบบมีผู้ช่วยในแอพ (ปุ่มลอยมุมล่างขวา ⚡) ทำงานได้ 2 โหมด:

| | โหมดฟรี (เปิดอยู่แล้ว) | โหมด Claude AI (ตั้งค่าเพิ่ม) |
|---|---|---|
| ค่าใช้จ่าย | ฟรี 100% | จ่ายตามใช้จริง (ทีมเล็ก ~หลักสิบ-หลักร้อยบาท/เดือน) |
| ตอบจาก | ชุดคำถามที่ระบบเข้าใจ | เข้าใจภาษาอิสระ วิเคราะห์/สรุป/เปรียบเทียบได้ |
| ความเร็ว | ทันที ใช้ offline ได้ | ~2-5 วินาที ต้องออนไลน์ |
| ติดตั้ง | ไม่ต้องทำอะไร | ~10 นาที ครั้งเดียว (ด้านล่าง) |

**ทั้งสองโหมดเคารพสิทธิ์ผู้ใช้** — ผู้ช่วยเห็นเฉพาะคำขอที่คนถามมีสิทธิ์เห็นในแอพเท่านั้น

## โหมดฟรี — ใช้ได้เลย ถามอะไรได้บ้าง

- สถานะคำขอ: พิมพ์รหัส เช่น `WR-26-0341` / "คำขอของฉันถึงไหนแล้ว"
- งานค้าง: "รอฉันอนุมัติกี่รายการ" / "มีอะไรเกินกำหนดบ้าง" / "ค้างเคลียร์เอกสาร"
- ตัวเลข: "ยอดเบิกเงินเดือนนี้เท่าไหร่" / "จัดซื้อเดือน มิ.ย. กี่รายการ"
- อื่นๆ: "สต๊อกวิตามินซีเหลือเท่าไหร่" / "ใครอยู่แผนกการตลาดบ้าง" / ค้นคำในหัวข้อคำขอ

ผลลัพธ์ที่เป็นรายการคำขอ **แตะเพื่อเปิดหน้ารายละเอียดได้เลย**

---

# เปิดโหมด Claude AI (ครั้งเดียว ~10 นาที)

หลักการ: ห้ามใส่ API key ในหน้าเว็บ (ใครก็แกะได้) จึงใช้ **Cloudflare Worker** (ฟรี 100,000 ครั้ง/วัน)
เป็นตัวกลางเก็บ key — โค้ดเตรียมให้แล้วที่ `cloudflare/worker.js`

## ขั้นที่ 1 — ขอ API key จาก Anthropic

1. สมัคร/ล็อกอินที่ <https://console.anthropic.com>
2. เมนู **Billing** → เติมเครดิต (เริ่มต่ำสุด $5 — พอใช้ทีมเล็กหลายเดือน)
3. เมนู **API Keys** → **Create Key** → คัดลอกเก็บไว้ (ขึ้นต้น `sk-ant-...`)

## ขั้นที่ 2 — สร้าง Cloudflare Worker

1. สมัคร/ล็อกอินที่ <https://dash.cloudflare.com> (ฟรี ไม่ต้องใช้บัตร)
2. เมนูซ้าย **Workers & Pages** → **Create** → **Create Worker**
3. ตั้งชื่อ เช่น `flowdesk-ai` → **Deploy** (ได้ worker เปล่าก่อน)
4. กด **Edit code** → ลบโค้ดเดิมทั้งหมด → **วางโค้ดจากไฟล์ `cloudflare/worker.js` ทั้งไฟล์** → **Deploy**
5. กลับหน้า worker → แท็บ **Settings** → **Variables and Secrets** → เพิ่ม:
   - `ANTHROPIC_API_KEY` (ชนิด **Secret**) = key จากขั้นที่ 1
   - `TEAM_KEY` (ชนิด Secret, แนะนำ) = รหัสลับของทีม ตั้งเองอะไรก็ได้ เช่น `wellgate-2026`
     (กันคนนอกที่เดา URL เจอ มายืมใช้ AI ของเรา)
6. คัดลอก **URL ของ worker** เช่น `https://flowdesk-ai.xxx.workers.dev`

## ขั้นที่ 3 — เปิดใช้ในแอพ

1. เปิด FlowDesk → กดปุ่มผู้ช่วย ⚡ มุมล่างขวา → กดไอคอน **⚙** บนหัวแชท
2. วาง **Worker URL** + **Team key** (ถ้าตั้งไว้) → **บันทึก**
3. หัวแชทจะขึ้น **"✦ Claude AI"** — ถามได้อิสระเลย เช่น
   - "สรุปภาพรวมคำขอเดือนนี้ให้หน่อย มีอะไรน่ากังวลบ้าง"
   - "เปรียบเทียบยอดเบิกเงินกับจัดซื้อ อันไหนเยอะกว่า"
   - "ช่วยร่างข้อความตามงานคำขอที่ค้างนานให้หน่อย"

> การตั้งค่าเป็นรายเครื่อง — แจก URL + Team key ให้ทีมตั้งเองครั้งเดียวต่อเครื่อง
> ถ้า AI ล่ม/หมดเครดิต ระบบ**ถอยกลับโหมดฟรีอัตโนมัติ** พร้อมแจ้งเหตุผล ไม่มีทางใช้งานไม่ได้

## คำถามพบบ่อย

**ค่าใช้จ่ายประมาณเท่าไหร่?**
ใช้รุ่น Claude Haiku (ค่าเริ่มต้น) คำถามละ ~2,000-8,000 token ≈ ไม่กี่สตางค์ต่อคำถาม
ทีม 50 คนถามรวมวันละ 100 คำถาม ≈ หลักสิบ-ร้อยบาท/เดือน · ดูยอดจริงได้ที่ console.anthropic.com → Usage

**ข้อมูลอะไรถูกส่งออกไปบ้าง?**
เมื่อถามในโหมด AI ระบบส่ง "สรุปข้อมูลย่อ" (สถิติ + คำขอที่เกี่ยวข้อง ตัดที่ ~6,000 ตัวอักษร
เฉพาะที่ผู้ถามมีสิทธิ์เห็น) ไปยัง Worker ของคุณ → Anthropic API เพื่อสร้างคำตอบ
โหมดฟรีไม่ส่งอะไรออกนอกเครื่องเลย

**อยากเปลี่ยนรุ่น AI ให้ฉลาดขึ้น?**
เพิ่มตัวแปร `MODEL` ใน Worker เช่น `claude-sonnet-5` (ฉลาดขึ้น แพงขึ้น) — ไม่ตั้ง = Haiku

**ปิดโหมด AI กลับเป็นฟรี?**
เปิด ⚙ ในแชท → ลบ URL → บันทึก
